# @bindgen_tool — nixpkgs rust-bindgen CLI (prebuilt), for the bindgen toolchain.
package(default_visibility = ["//visibility:public"])

filegroup(name = "all", srcs = glob(["**"], allow_empty = False))

filegroup(name = "bin", srcs = ["bin/bindgen"])
