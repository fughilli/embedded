# @clang_tool — nixpkgs clang, for the bindgen toolchain's `clang` attr.
package(default_visibility = ["//visibility:public"])

filegroup(name = "all", srcs = glob(["**"], allow_empty = False))

filegroup(name = "bin", srcs = ["bin/clang"])
